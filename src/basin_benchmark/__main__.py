#!/usr/bin/env python3
"""Entry point: `python -m basin_benchmark`, `basin-benchmark` CLI, or direct execution."""

from basin_benchmark.cli import main

if __name__ == "__main__":
    raise SystemExit(main())
